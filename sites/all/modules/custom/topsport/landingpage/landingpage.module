<?php

function landingpage_menu(){
    $items['home'] = array(
			'title' => 'Home page',
			'page callback' => 'landingpage_Homepage',
			'access arguments' => array('access content'),
			'type' => MENU_NORMAL_ITEM,
	);
    return $items;
}

function  landingpage_Homepage(){
    $html  = '';
    return $html;
}

